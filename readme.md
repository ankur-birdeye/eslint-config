# @birdeye/eslint-config

## Usage

* First install the package as dev-dependency using
```
npm install -D @birdeye/eslint-config
or
yarn add -D @birdeye/eslint-config
```

* Create an .eslintrc file with the below content
```
{
  "extends": "@birdeye/eslint-config"
} 
```

